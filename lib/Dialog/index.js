import DialogConfirm from './Confirm'
import DialogAlert from './Alert'

export const Alert = DialogAlert
export const Confirm = DialogConfirm

export default {
  Alert: DialogAlert,
  Confirm: DialogConfirm
}
